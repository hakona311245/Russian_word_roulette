# Russian Word Roulette

A static Vite + React + TypeScript app for manual sentence translation
practice. Sentences are loaded from `public/data/sentences.json`; all state is
local React state.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The app is deployable to Vercel as a static Vite project.
